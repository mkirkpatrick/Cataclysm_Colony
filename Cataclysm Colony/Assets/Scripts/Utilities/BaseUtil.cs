using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BaseUtil {

    /*public static GameObject GetPlotObject(Plot plot, GameObject baseObj) {

        GameObject plotObj = baseObj.transform.Find("Plots").gameObject;
        GameObject groupObj;

        switch (plot.plotType)
        {
            case Plot.PlotType.Bunker:
                return plotObj.transform.Find("BunkerPlot").gameObject;
            case Plot.PlotType.Tier1:
                groupObj = plotObj.transform.Find("Inner Plots").gameObject;
                return groupObj.transform.Find("InnerPlot" + plot.plotNum).gameObject;
            case Plot.PlotType.Tier1Wall:
                groupObj = plotObj.transform.Find("Inner Wall Plots").gameObject;
                return groupObj.transform.Find("InnerWallPlot" + plot.plotNum).gameObject;
            case Plot.PlotType.Tier2:
                return null;
            case Plot.PlotType.Tier2Wall:
                return null;
            case Plot.PlotType.Wilderness:
                return null;
            default:
                return null;
        }

    }
*/
}
